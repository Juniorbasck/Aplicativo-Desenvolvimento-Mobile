import {
    View,
    StyleSheet
} from 'react-native';

const CustomDropdown = ({state, setState, options=['']}) => {
    return (
        <View>

        </View>
    );
};

const styles = StyleSheet.create({

});

export { CustomDropdown };
